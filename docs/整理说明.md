# 文件夹整理说明

> 整理目标目录：`C:\Users\q\Desktop\000\重命名`
> 整理时间：2026-08-27
> 原则：按软件开发阶段（需求→设计→实现→测试→构建→部署→同步）归类；仅保留每类文件最新版本；与 GitHub 远程仓库保持源码逐字节一致。

---

## 一、GitHub 源码一致性核对结果

保留的新源码与昨天上传到 GitHub 仓库的源码（`闪电重命名_LightningRename_v1.0.0\LightningRename`）：

- 文件数量、文件名：**完全一致**（10 个文件）。
- 9 个源文件：**哈希完全一致**。
- `MainForm.cs`：仅文件末尾**少 1 个换行符 `0A`**（逐行内容相同）。
- 已归一化：给保留版补末尾换行符，使其与 GitHub 版**逐字节完全一致**（hash `BC12BF08...`）。
- 复验：归一化后 10 个源文件全部逐字节一致，通过。

---

## 二、保留文件与最终结构（共 30 项）

```
重命名\
├─ 00_规范文档\
│   ├─ GitHub上传教程.md
│   ├─ 发布流程规范.md
│   ├─ 安全审查报告.md
│   └─ win-sdk-0d9b8ba0-....png
├─ 01_源码\LightningRename\      ← 与 GitHub 一致的最新源码
│   ├─ app.ico / app.manifest
│   ├─ Engine.cs / Rules.cs / Item.cs / UndoLog.cs
│   ├─ MainForm.cs / Program.cs
│   ├─ LightningRename.csproj
│   └─ Properties\AssemblyInfo.cs
├─ 02_测试\
│   ├─ 测试报告.html
│   ├─ run_tests.ps1
│   ├─ TestHarness\TestMain.cs
│   ├─ TestHarness\TestHarness.exe
│   └─ sample\（4 个测试样例）
├─ 03_构建\
│   └─ build.ps1（已改 UTF-8 BOM + 适配新路径）
├─ 04_发布\
│   └─ 闪电重命名_v1.0.0.exe
├─ 05_GitHub同步\
│   ├─ README.md
│   ├─ CHANGELOG.md
│   ├─ LICENSE
│   ├─ .gitignore
│   └─ LightningRename.sln
└─ 06_工具安装包\
    └─ dotnet-sdk-8.0.424-win-x64.exe
```

---

## 三、删除文件清单及判断依据

| 删除项 | 判断依据 |
|---|---|
| `闪电重命名_v2.1.exe` | 旧版 v2.1，被重制版取代 |
| `闪电重命名_v2.1_重制版.exe`、`LightningRename_v2.1_remake.exe` | 重制版早期中间构建，被最终版取代 |
| 3 份重复 `闪电重命名_v1.0.0.exe` | 与 `04_发布` 规范副本哈希完全相同 |
| 两份旧源码镜像（上传包内 `LightningRename\` 源目录） | 内容被 `01_源码` 新版取代 |
| 被取代的旧 README/LICENSE/.gitignore/build.ps1 | 旧包重复文件 |
| `dotnet-sdk--win-x64.exe`(0.2KB)、`msopenjdk21.msi`(14.6KB) | 损坏的半截下载 |

---

## 四、执行过程中的一处疏漏（如实记录）

处理上传包内 `tests\` 目录时，哈希函数名与 PowerShell 内置 `Get-History` 别名冲突，导致"一致=True"为脚本假阳性，`tests\` 副本据此被删除。

**影响评估**：被删的只是上传包内的重复测试镜像；规范版 `02_测试\run_tests.ps1` 与 `02_测试\TestHarness\TestMain.cs` 完好保留，被删副本与其同尺寸（1.1KB / 18.6KB）。项目未受损；但该删除未经严格哈希验证，仅为高置信推断。

---

## 五、后续优化建议

1. `build.ps1` 已修复（UTF-8 BOM + 指向 `01_源码`、输出到 `04_发布`），实测构建成功。
2. 建议在 `05_GitHub同步` 重新 `git init` 统一提交，以 `01_源码` 为主推送源，保持与远程一致。
3. `02_测试\sample` 为 0KB 占位，建议补充真实测试采样。
4. 建议将 `06_工具安装包` 移出项目文件夹，避免仓库体积过大。
5. 每次发布按《发布流程规范》打语义化 tag 并复核 SHA-256。