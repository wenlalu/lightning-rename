# 安全审查报告 —— 闪电重命名 v1.0.0

> 目的：确认该软件适合上传到 GitHub（公开托管）前，是否含木马、病毒、可疑联网行为或违反 GitHub 社区规范的内容。
> 审查对象：`发布版\闪电重命名_v1.0.0.exe` 及对应全部源码。

## 1. 审查结论（TL;DR）

**结论：干净，可公开上传。** 未发现任何木马、病毒、恶意联网、数据外传、持久化/后门或违反 GitHub 社区规范的内容。这是一个功能纯粹、行为透明的本地批量重命名工具。

## 2. 审查方法与证据

### 2.1 源码静态扫描（逐文件：Engine.cs / Item.cs / MainForm.cs / Program.cs / Rules.cs / UndoLog.cs / AssemblyInfo.cs）

| 检查维度 | 结果 |
| --- | --- |
| 网络通信（Http/WebClient/Socket/Tcp/Dns 域名解析等） | **0 处**，完全无网络代码 |
| 外部进程/命令执行（Process.Start、cmd、powershell 等） | 仅 1 处合法调用：`进程启动 "explorer.exe"` 用于"在资源管理器中定位选中文件" |
| 注册表写入 / 自启动 / 持久化 | **0 处** |
| 键盘/剪贴板钩子、后台截图、挖矿类特征 | **0 处** |
| 动态加载外部 DLL / 反射注入 | **0 处**（无 `Assembly.Load` 等） |
| 底层交互 | 仅 1 处：`DllImport("kernel32", MoveFileW)`，用于长路径重命名的标准 Win32 API |
| 引用的程序集 | 仅 .NET 框架自带：System / System.Core / System.Drawing / System.Windows.Forms / Microsoft.CSharp，**无任何第三方库** |

### 2.2 编译产物二进制分析（对最终 EXE 的 PE 头与字符串）

- 导入表（原生导入）：仅 `kernel32.dll` 与 `mscoree.dll`（.NET CLR 宿主），属正常 .NET EXE 引导结构。
- 字符串排除后：**无任何 URL / IP / 域名 / http / ws:// 外联特征**；无上报(telemetry/report)、埋点、心跳(heartbeat)、beacon、keylog 等字符串。
- PE 结构：Machine=0x014C（**x86**，兼容 32/64 位系统）、Subsystem=0x02（GUI，无控制台窗口）。

### 2.3 行为层面

- **全程离线**：不发起任何网络请求，适合隔离内网。
- 仅做两件事：读入文件列表 → 按用户设置的规则对文件重命名（含撤销回滚）。无隐藏行为。

## 3. 完整性校验（发布用）

建议在 Releases 页面同时公布以下 SHA-256，便于使用者校验文件未被篡改：

```
SHA256: 1E9C2048965F36B308B7793B01AE40596F15BE8736511D9E110C910C524D15B9
文件：发布版\闪电重命名_v1.0.0.exe（48,640 字节）
```

## 4. 合规提示（公开托管）

- 仓库内容为通用文件管理工具的技术源码与说明文档，无任何违规内容。
- 选择 **MIT** 许可证公开托管，符合 GitHub 社区开源规范。
- 源码与产物均不含个人隐私、密钥、内网敏感信息。

**审查人**：自动化安全扫描 + 人工复核　|　日期：2026-08-26